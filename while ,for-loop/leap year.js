
/*

function leapYear(year) {
    const leapYears = year % 4;
    // return leapYears;
    if (leapYears == 0) {
        console.log('leap Year');
    }
    else {
        console.log('Not a leap Year');

    }
}
leapYear(1971);
leapYear(1960);

function leapYear(year) {
    const leapYears = year % 4;
    // return leapYears;
    if (leapYears == 0) {
        return true;
    }
    else {
        return false;
    }
}
const myYear = leapYear(1972);
console.log(myYear);
const herYear = leapYear(1971);
console.log(herYear);


*/